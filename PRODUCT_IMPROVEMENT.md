# Always Here 产品级完善方案与任务清单

> 生成日期：2026-07-29
> 视角：从"功能完整"升级为"会陪伴的产品"。聚焦用户能直接感知的体验,而非代码实现。
> 基于当前 main 分支(commit `ffe28be`)的代码事实核实。

---

## 一、产品现状诊断

### 已经做得好的
- **定位清晰**：透明置顶层 + 5 挂件 + 会说话的伙伴,"陪伴 + 实用"的差异化成立。
- **功能丰富**：番茄钟、打工倒计时、语音对话、看屏幕、好感度、多主题、自动更新,功能面已超过多数同类。
- **工程扎实**：86 个单测全过、contextIsolation 安全模型、AudioWorklet、配置迁移机制。

### 产品层面的三个核心短板
1. **第一印象"裸奔"**：首启直接铺开 5 个挂件,无引导;小屏坐标溢出(已有视口 clamp 兜底,但仍显凌乱)。
2. **挂件是"孤岛"**：5 个工具各自为政,伙伴没有把它们串成一个整体。
3. **情感闭环缺失**：积累了行为数据(喝水/番茄/收入),但从没变成"伙伴对你说的话"。

**一句话**：功能够了,差的是把它从"一堆桌面小工具"变成"一个真正陪着我的伙伴"。

---

## 二、方案总览(按性价比排序)

| 编号 | 任务 | 影响 | 工作量 | 阶段 |
|---|---|---|---|---|
| **T1** | 今日回顾(情感闭环) | ⭐⭐⭐ | 小(~60 行) | **第一期** |
| **T2** | 伙伴作为信息中枢(串联挂件) | ⭐⭐⭐ | 中(~80 行) | **第一期** |
| **T3** | 首启引导与欢迎气泡 | ⭐⭐⭐ | 中(~70 行) | **第一期** |
| **T4** | 默认布局按屏幕自适应 | ⭐⭐ | 小(~30 行) | **第一期** |
| T5 | 数据导出/导入(换机迁移) | ⭐⭐ | 小(~50 行) | 第二期 |
| T6 | 预设布局模式 | ⭐⭐ | 中(~90 行) | 第二期 |
| T7 | 伙伴素材入口前置 | ⭐⭐ | 小(~20 行) | 第二期 |
| T8 | 设置面板搜索 | ⭐ | 中(~70 行) | 第三期 |
| T9 | 多屏/副屏支持 | ⭐ | 大 | Roadmap |

**第一期(T1-T4)是性价比最高的组合**：用约 240 行代码,把"首印象、统一感、情感闭环"三个核心短板一次性补齐。

---

## 三、第一期详细方案(立即实施)

### T1　今日回顾(情感闭环) — 最优先

**问题**：每天积累的数据(番茄/喝水/收入)只是冷冰冰的记录,从不变成温暖的反馈。

**方案**：
- 新建 `petDailyRecap.mjs`,提供 `shouldShowDailyRecap(config)` 与 `buildRecapPrompt(config)`。
- **触发时机**：每天首次满足以下任一条件时,伙伴主动说一句回顾:
  - 打工下班后(`WORK_STOP` 事件,当天首次)
  - 或晚上 20:00 后首次互动(给不上班的人兜底)
- **去重**：用 `config.lastRecapDate`(YYYY-M-D)记录"今天已回顾过",一天最多一次。
- **内容**：基于 `summarizeRecentDays(log, 1)` 拼接,例如:
  > "今天你专注了 3 个番茄钟、完成了 2 次喝水提醒,辛苦啦~"
- **走 AI**：语音已启用时走小智 LLM 生成个性化台词;否则用本地模板。

**改动文件**：
- 新建 `widgets/petDailyRecap.mjs`(纯函数,可单测)
- `widgets/pet.js`：WORK_STOP 监听里调用;新增 20:00 检查定时器
- `main.js` DEFAULT_CONFIG 加 `lastRecapDate: null`
- 需在 `activityStats.mjs` 的 `summarizeActivityLog` 补充"今日番茄完成数"统计

**验证**：单测 `shouldShowDailyRecap`(当天已回顾→false)、`buildRecapPrompt` 生成文本含番茄/喝水数。

---

### T2　伙伴作为信息中枢(串联挂件)

**问题**：打工"快下班"、便签"有待办"、番茄完成,各自弹通知或气泡,视觉碎片化。伙伴应是唯一"发声口"。

**方案**：把分散的事件**统一收口到伙伴气泡**,减少系统通知的碎片感:
- **便签待办提醒**：便签里有未完成 `- [ ]` 且超过 N 小时未编辑 → 伙伴说"便签里还有 3 件事没做,要不要先处理一件?"(复用已有的 `countNoteTodos`,T1 的定时器里顺带检查)
- **打工快下班**：已有 `work-off-soon` 提醒(下班前 10 分钟),改为**只走伙伴气泡**,关闭其 systemNotification 默认值,避免"系统通知 + 气泡"双重打扰。
- **喝水/久坐**：保持伙伴气泡(已是),但把"系统通知"默认关掉(当前默认就是 false,确认即可),让伙伴成为唯一提醒源。

**改动文件**：
- `widgets/petDailyRecap.mjs` 的定时检查里顺带做便签待办提醒(复用 T1 的触发点)
- `widgets/petReminders.mjs`：确认 work 提醒的 systemNotification 默认 false
- 不新增大量代码,主要是"收口"逻辑

**验证**：手动测试——便签留待办、到点伙伴是否提示;打工快下班是否只有气泡无系统通知。

**设计原则**：伙伴是"前台发言人",系统通知只在用户离开桌面(气泡看不见)时才有价值——可保留开关,但默认让伙伴说话。

---

### T3　首启引导与欢迎气泡

**问题**：首启无引导,用户不知道能右键/拖动/对话。

**方案**：
- 检测 `config.configVersion` 升级路径,新增 `config.hasOnboarded` 标志(首启为 false)。
- 首启时:
  - 伙伴气泡延迟 2 秒弹出:"你好呀!我是 XX。右键我可以打开设置,拖动我能换个位置~"(8 秒后消失)
  - 用户首次右键/打开设置后,置 `hasOnboarded = true`。
- **不强行全屏引导**(那样太重),用伙伴自然的"自我介绍"完成 onboarding,符合产品调性。

**改动文件**：
- `main.js` DEFAULT_CONFIG 加 `hasOnboarded: false`
- `widgets/pet.js`：initPet 末尾,若 `!config.hasOnboarded` 则延迟弹欢迎气泡
- `settings.js`：首次打开设置时置 `hasOnboarded = true` 并保存

**验证**：清空 config 重启,伙伴应弹欢迎气泡;打开设置后重启不再弹。

---

### T4　默认布局按屏幕自适应

**问题**：DEFAULT_WIDGETS 坐标写死,小屏(1280×800)首次启动挂件挤在一起(虽有视口 clamp 兜底,但布局不合理)。

**现状**：`applyWidgetPositions` 已有 clamp(line 89-90),溢出会被拉回视口,不会"看不见"。但 clamp 后的布局可能重叠、不美观。

**方案**：
- `initConfig` 里,若检测到是**首次启动**(`!config.hasOnboarded` 或 `configVersion` 从 0 升级),按屏幕宽度选择预设布局:
  - `width >= 1600`：当前布局(宽屏铺开)
  - `width < 1600`：紧凑布局——挂件靠左/右两侧纵向排列,避免中部拥挤
- 复用 T6 的"预设布局"基础设施(可先实现两个预设)。

**改动文件**：
- `utils/config.js`：initConfig 里首启时按 `getScreenSize()` 选布局
- 新增 `utils/widgetLayouts.mjs`：定义 2-3 套布局坐标(纯数据,可单测)

**验证**：单测不同屏幕宽度返回不同布局;1280 屏首启挂件不重叠。

---

## 四、第二期方案(尽快实施)

### T5　数据导出/导入

**问题**：换电脑好感度、对话历史、便签全丢。陪伴类产品这是信任硬伤。

**方案**：
- 主进程加 IPC：`export-all-data`(打包 config.json + history.jsonl 为 zip)、`import-all-data`(解压并覆盖)。
- 设置→系统 tab 加"导出我的数据""导入数据"按钮。
- 导入前用 `showConfirm` 二次确认(会覆盖现有数据)。

**工作量**：~50 行(用 Node 内置或 electron 的对话框 + 文件读写)。

---

### T6　预设布局模式

**问题**：5 个挂件逐个开关,认知负担重。

**方案**：
- 设置→组件 tab 加 3 个预设按钮："极简(时钟+伙伴)""办公(时钟+便签+打工)""全能(全开)"。
- 点击后批量改 `config.widgets[key].enabled` 并 `applyWidgetPositions`。
- 复用 T4 的 `widgetLayouts.mjs`。

**工作量**：~90 行。

---

### T7　伙伴素材入口前置

**问题**：伙伴素材下载入口藏在设置深处,生态优势没发挥。

**方案**：
- 伙伴首次互动后,气泡偶尔(如每 7 天一次)提示"想换个伙伴吗?右键我看看"。
- 设置→伙伴管理器顶部加显眼的"去伙伴商店看看"按钮。

**工作量**：~20 行。

---

## 五、第三期 / Roadmap

### T8　设置面板搜索
设置顶部加搜索框,输入关键词定位到对应开关/tab。对"设置项多"的产品是标配。~70 行。

### T9　多屏/副屏支持
允许选择挂件所在屏幕。当前只绑主屏。工作量较大,涉及多 BrowserWindow 或坐标映射,列为 Roadmap。

---

## 六、任务清单(可执行)

### 第一期(本次实施,约 240 行)

- [ ] **T1-a** `activityStats.mjs`：`summarizeActivityLog` 补充今日番茄完成数(`pomodoro-done` 计数)
- [ ] **T1-b** 新建 `widgets/petDailyRecap.mjs`：`shouldShowDailyRecap` + `buildRecapPrompt`(纯函数)
- [ ] **T1-c** `main.js`：DEFAULT_CONFIG 加 `lastRecapDate: null`
- [ ] **T1-d** `pet.js`：WORK_STOP 监听 + 20:00 定时器触发回顾;去重
- [ ] **T1-e** 单测 `petDailyRecap.test.js`：去重、prompt 生成
- [ ] **T2-a** 确认 `petReminders.mjs` 的 work 提醒 systemNotification 默认 false
- [ ] **T2-b** `petDailyRecap.mjs` 定时检查里加便签待办提醒(复用 countNoteTodos)
- [ ] **T3-a** `main.js`：DEFAULT_CONFIG 加 `hasOnboarded: false`
- [ ] **T3-b** `pet.js`：首启延迟弹欢迎气泡;首次打开设置后置 true
- [ ] **T3-c** `settings.js`：首次打开设置时置 `hasOnboarded = true`
- [ ] **T4-a** 新建 `utils/widgetLayouts.mjs`：宽屏/紧凑两套布局
- [ ] **T4-b** `config.js`：首启按 `getScreenSize` 选布局
- [ ] **T4-c** 单测 `widgetLayouts.test.js`：不同宽度返回合理坐标
- [ ] **验证** 语法检查 + `node --test` 全过 + 手动跑应用

### 第二期(后续)

- [ ] T5 数据导出/导入 IPC + UI
- [ ] T6 预设布局模式(复用 widgetLayouts)
- [ ] T7 伙伴素材入口前置

### 第三期 / Roadmap

- [ ] T8 设置面板搜索
- [ ] T9 多屏/副屏支持

---

## 七、设计原则(贯穿所有任务)

1. **伙伴是唯一前台发言人**：能用伙伴气泡说的,就不弹系统通知(系统通知只用于用户可能离开桌面时)。
2. **数据要变成反馈**：任何积累的数据(好感度/行为/对话),都要找机会变成伙伴对用户说的话,否则数据没有情感价值。
3. **首启要轻**：不搞全屏引导遮罩,用伙伴自然的自我介绍完成 onboarding。
4. **新增逻辑尽量纯函数化**：petDailyRecap / widgetLayouts 都做成纯函数 + 单测,保持现有工程标准。

---

## 八、预期效果

完成第一期后,用户的一天会变成这样:

> 早上开机 → 伙伴:"早安~昨天你专注了 4 个番茄钟,今天继续加油!"(T1 回顾)
> 工作中 → 便签有未完成待办 → 伙伴:"便签里还有 2 件事,先做哪个?"(T2 收口)
> 快下班 → 伙伴:"再坚持一下,马上下班啦"(T2,只有气泡,无系统通知打扰)
> 下班 → 伙伴:"今天赚了 320 元,辛苦啦,好好休息"(T1 回顾)
> 新用户首启 → 伙伴:"你好呀!我是 XX,右键我可以设置哦"(T3 引导)
> 小屏笔记本 → 挂件整齐排布,不溢出(T4 自适应)

从"一堆桌面工具"变成"一个陪你一整天的伙伴"。
